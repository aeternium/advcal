[project]
name = "advcal"
version = "0.1.0"
description = "super simple image advent calendar"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "nicegui>=2.2.0",
]

[tool.uv]
dev-dependencies = [
    "ruff>=0.6.8",
]
